{
  "name": "user-experience-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently -n FRONTEND,BACKEND -c \"bgBlue.bold,bgGreen.bold\" \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
