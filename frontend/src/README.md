# Frontend structure

- `app/`: composition root (layouts, providers, routing)
- `assets/`: static assets (images, fonts, svg)
- `core/`: config, constants, hooks, cross-cutting services
- `features/`: vertical slices (auth, dashboard, etc.)
- `shared/`: reusable primitives
- `store/`: global Zustand stores and middlewares
